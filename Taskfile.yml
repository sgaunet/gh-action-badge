---
# https://taskfile.dev
version: '3'
tasks:
  default:
    desc: "List tasks"
    cmds:
      - task -a

  build:
    desc: "Build Docker image locally (no push)"
    cmds:
      - docker build . -t gh-action-badge:dev
